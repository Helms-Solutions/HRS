export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.OPENAI_API_KEY)return res.status(503).json({error:'OPENAI_API_KEY is not configured'});try{const{topic='',template='',audience='',market='',details=''}=req.body||{};if(!topic.trim())return res.status(400).json({error:'Topic is required'});const prompt=`Create a polished square Facebook social media graphic for Helms Retirement Solutions. Topic: ${topic}. Campaign type: ${template}. Audience: ${audience}. Market: ${market}. Extra context: ${details||'none'}. Brand style: trustworthy, local, approachable retirement and Medicare education; navy blue, medium blue, white, restrained gold accents; crisp modern layout; realistic professional lifestyle imagery where appropriate; strong visual hierarchy; generous whitespace; mobile-feed readable. Include only a short headline of no more than 8 words based on the topic. Do not include carrier logos, government seals, Medicare cards, plan-specific benefits, prices, guarantees, or dense fine print. Do not depict identifiable politicians or government officials. Avoid spelling errors and do not add fake contact information.`;const response=await fetch('https://api.openai.com/v1/images/generations',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-image-1',prompt,size:'1024x1024',quality:'medium',output_format:'png'})});const data=await response.json();if(!response.ok)throw new Error(data.error?.message||'OpenAI image request failed');const image=data.data?.[0]?.b64_json;if(!image)throw new Error('No image returned');res.setHeader('Cache-Control','no-store');return res.status(200).json({image})}catch(error){console.error(error);return res.status(500).json({error:error.message||'Unable to create image'})}}